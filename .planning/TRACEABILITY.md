# Traceability

Statuses: **PROVEN** has concrete evidence, **ASSERTED** is claimed complete without located proof, **OPEN** is not complete, and **WAIVED** is an explicitly accepted gap.

Independent v1.2.2 close-out sweep 2026-07-24 (frontier auditor; authored no v1.1/v1.2/v1.2.1/v1.2.2 slice). Existing PROVEN labels and checked boxes were treated as claims and re-verified against source, tests, and a fresh gate run. Saga structure lint (`saga-lint --format json`) re-run fresh: `clean: true`, 0 findings, exit 0. `npm run check` (typecheck + full in-tmux integration suite + `npm audit --omit=dev`) re-run fresh by the auditor: exit 0. All seven suites printed their pass lines (`tmux-job manager smoke`, `Hermes native configuration inheritance`, `bounded, unlimited, and byte-exact chunked log-writer`, `tmux_agent completion notifier`, `live Pi completion delivery and shutdown suppression`, `packed-package install and tool discovery`, `tmux_job and tmux_agent extension discovery and invocation`); `found 0 vulnerabilities`. The v1.2.2 delta touches no production source (`index.ts`, `agent-adapters.ts`, `completion-notifier.ts`, `job-manager.ts`, `model-registry.ts`, `log-writer.mjs` are byte-identical to HEAD/v1.2.1); it adds the real-AgentSession live-completion test, wires it into the harness, bumps the version to 1.2.2, and updates docs. Evidence below is located in the named suites and freshly reproduced by that run unless noted.

| Requirement | Description | Status | Evidence |
|---|---|---|---|
| REQ-001 | Commands keep a real PTY with durable complete output and exit state | **PROVEN** | `test-manager.mjs` starts a job asserting `[ -t 0 ] && [ -t 1 ] && [ -t 2 ]`, exit 23; verifies `state=exited`, exitCode 23, durable `output.log` with ordered `req001-first` < `req001-last`, `req001-tty-ok`, and `[tmux-job] finished=… exit=23`. Drain protocol in `job-manager.ts` gates state flip on logger-ready and pipe-drained sentinels. Freshly reproduced 2026-07-24 |
| REQ-002 | Separate structured launcher supports Pi, Claude Code, and Hermes | **PROVEN** | `test-extension.mjs` asserts `tmux_agent` registered without a `maxLogBytes` override; backend×mode command construction and owned-job discovery exercised through fake executables; `index.ts:235-307` registers a distinct `tmux_agent` tool that reuses the owned-pane lifecycle via `manager.start`. Freshly reproduced 2026-07-24 |
| REQ-003 | Prompt transport is private where supported; documents the Hermes argv exception | **PROVEN** | `agent-adapters.ts` feeds pi/claude prompts over stdin (`input.txt`, mode 0600 in `job-manager.ts`) while Hermes `--oneshot "$(<…)"` exposes argv; `test-extension.mjs` inspects live `/proc/$$/cmdline` and persisted artifacts; README + `index.ts:241,290-293` surface the exception. Freshly reproduced 2026-07-24 |
| REQ-004 | Adapters preserve native permissions and surface Hermes one-shot auto-bypass | **PROVEN** | `test-extension.mjs` asserts no bypass flags across every backend/mode; `test-hermes-config.mjs` re-asserts absence of isolation/bypass flags; Hermes dispatch warning `index.ts:290-293`. Freshly reproduced 2026-07-24 |
| REQ-005 | Interactive agents remain observable and controllable through tmux | **PROVEN** | `test-extension.mjs` runs long-lived TTY fakes through bounded wait (timedOut while running), send, tail, interrupt, then completed wait asserting exit 130; controls in `job-manager.ts`. Freshly reproduced 2026-07-24 |
| REQ-006 | Dispatch completion notifies the originating live session exactly once | **PROVEN** | `test-notifier.mjs` proves single delivery + duplicate suppression, bounded content/`logPath`, backend/exit fields, `deliverAs:"followUp"`/`triggerTurn`, idempotent shutdown abort (`completion-notifier.ts:34-90`); `test-extension.mjs` confirms dispatch returns while launching/running and one `session_shutdown` handler (`index.ts:75`). The prior "unit + wiring only, no observed live turn" boundary carried across v1.1/v1.2/v1.2.1 is now **closed** by REQ-014, which drives the same `pi.sendMessage`/`session_shutdown` wiring through a real in-memory Pi `AgentSession` and observes the delivered follow-up turn. Freshly reproduced 2026-07-24 |
| REQ-007 | Existing generic tmux job behavior and safeguards remain compatible | **PROVEN** | `test-manager.mjs` covers unowned-pane filtering, duplicate-name rejection, start/wait/capture/send, running-job close refusal, interrupt→nonzero exit, forced close; `test-extension.mjs` proves bounded model-facing output (`Output truncated`, <55KB). The `log=` field and truncation notices are additive; legacy suites remain green. Freshly reproduced 2026-07-24 |
| REQ-008 | Fresh installs expose and document tool surfaces, model routing, and boundaries | **PROVEN** | `test-package.mjs` packs v1.2.2, installs offline into a clean root, asserts runtime files incl. `log-writer.mjs`, `manifest.provides.tools == [tmux_job, tmux_agent]`, `manifest.version == packageJson.version == "1.2.2"`, README coverage of backends, `litellm/deep`, `PI_TMUX_JOB_MAX_LOG_BYTES`, `log-truncated`, config inheritance, Hermes exception, watcher lifecycle, `## Non-goals`, and discovers both tools. Freshly reproduced 2026-07-24 |
| REQ-009 | Pi validates and routes GPT and local LiteLLM model selections | **PROVEN** | `test-extension.mjs` rejects missing/ambiguous models and routes valid `litellm/deep` through Pi; `model-registry.ts` enforces exact `provider/model`; `agent-adapters.ts` has no Codex CLI launch path. Freshly reproduced 2026-07-24 |
| REQ-010 | Hermes retains the operator's native configuration and customizations | **PROVEN** | `test-hermes-config.mjs` uses isolated `HOME` + executable override; both commands read the fixture `.hermes/config.yaml` while omitting `--safe-mode`/`--ignore-user-config`/`--ignore-rules`/`--yolo`/`--no-plugins`/`--disable-plugins`. Freshly reproduced 2026-07-24 |
| REQ-011 | Operator policy bounds retained per-job logs during execution | **PROVEN** | `log-writer.mjs` `writeBounded` truncates `output.log` to ≤`maxBytes` after every chunk (oversized-chunk keeps newest tail; in-budget appends; over-budget reads newest retained tail then rewrites), so the on-disk footprint is bounded continuously. `test-log-writer.mjs` byte-exactly verifies a single 100KB write retains the newest 4096 bytes (`deepEqual` to `subarray(-4096)`) and unlimited preserves all bytes. `test-manager.mjs` sets `PI_TMUX_JOB_MAX_LOG_BYTES=4096`, then during sustained real-tmux output samples all on-disk `output.log*` files 30× asserting ≤4096 with ≥3 non-empty running samples, confirms the newest marker survives, and proves unlimited growth and invalid-value rejection (`parseMaxLogBytes` in `job-manager.ts`). Policy is env/constructor-only: neither `tmux_job` nor `tmux_agent` exposes `maxLogBytes` (`test-extension.mjs`; schema in `index.ts`). Freshly reproduced 2026-07-24 |
| REQ-012 | Capped logging preserves PTY/completion behavior and reports truncation | **PROVEN** | Under a 4096 cap `test-manager.mjs` proves stdin/stdout/stderr TTYs (`REQ012_TTY_OK`, exit 0), finished marker in the durable log, `maxLogBytes=4096`, `logTruncated=true`, `log-truncated` marker file = `"true\n"`, and `metadata.json.maxLogBytes=4096`; drain-before-exit gate unchanged (`job-manager.ts`; `log-writer.mjs` writes drained only after a clean loop). Reporting is surfaced everywhere: `describeJob` `log=truncated:<bytes>` and `logRetentionNotice` (`index.ts:24-44,118-231`), completion notice "truncated to newest N bytes" (`completion-notifier.ts:61-80`, `test-notifier.mjs`), tool output "older terminal output was discarded" (`test-extension.mjs`), and README contract (asserted in `test-package.mjs`). Freshly reproduced 2026-07-24 |
| REQ-013 | Repeated small over-budget writes retain a byte-exact chronological suffix | **PROVEN** | `test-log-writer.mjs` streams ten distinct 71-byte chunks under a 128-byte cap, writing each chunk only after the on-disk file is confirmed byte-for-byte equal to the running `completeInput.subarray(-128)` via `waitForExactLog`. Chunk 0 appends and chunks 1–9 each trip `writeBounded` case 3 (read newest `128-71=57` B, truncate, rewrite `[retained tail][new chunk]`) — nine genuine repeated over-budget rewrites, each byte-checked mid-stream. Final assertions confirm the file equals the newest 128 bytes of the full 710-byte stream, size == 128, `truncated == "true\n"`, and `drained == "drained\n"`. `log-writer.mjs` source is unchanged since v1.2. Independently recomputed chunk arithmetic; freshly reproduced 2026-07-24 (`bounded, unlimited, and byte-exact chunked log-writer tests passed`) |
| REQ-014 | Live Pi completion delivery, follow-up turn, and shutdown suppression | **PROVEN** | `test-live-notification.mjs` loads the packaged `index.ts` extension into a real in-memory Pi `AgentSession` (`createAgentSession` + real `ModelRuntime`/`SessionManager`/`SettingsManager`/`DefaultResourceLoader`), asserts zero extension-load errors and that the extension and its `tmux_agent`/`tmux_job` tools registered. Scenario 1 drives `tmux_agent` dispatch through a scripted native `fauxProvider` and a real fake-`pi` tmux child (prompt over private stdin, exits ~0.35 s): it asserts exactly one `tool_execution_end` for `tmux_agent`, exactly one `tmux-agent-completion` custom session message whose `content` matches `job=<name>`, `agentStarts == 2` and `faux.callCount == 3` (exactly one follow-up run/model call), and — critically — a follow-up faux response that inspects `context.messages` and asserts the delivered completion (`Agent dispatch completed: backend=pi`, `job=<name>`) is present in the model's follow-up context before returning `COMPLETION_FOLLOW_UP_SEEN`; a 300 ms quiescence window plus the notifier's `delivered` set exclude duplicate delivery. Scenario 2 dispatches a slow (~1.2 s) child, calls real `session.reload()` (firing `session_shutdown` → `notifier.shutdown()`) before completion, then `tmux_job wait`s for the child to actually exit and asserts zero custom messages, `agentStarts` still 1, and `faux.callCount` still 2 — a genuine post-shutdown suppression proof, not a race. No production source changed vs HEAD; the exercised `pi.sendMessage`/`session_shutdown` wiring is the shipped path. `npm run check` exit 0 (all seven suites + 0 vulnerabilities), freshly reproduced by the auditor 2026-07-24 |

## Summary

- PROVEN: 14 — REQ-001 … REQ-014
- ASSERTED: 0
- OPEN: 0
- WAIVED: 0
- Freshly reproduced: Saga structure lint (`clean: true`, exit 0) and full `npm run check` (typecheck + all seven in-tmux suites incl. the real-AgentSession `live Pi completion delivery and shutdown suppression` test + `npm audit --omit=dev`, exit 0, 0 vulnerabilities), 2026-07-24.
- Located only / not independently rerun: none. The REQ-006 live-turn delivery boundary noted across prior close-outs is now closed by REQ-014's real-AgentSession proof.
- Unavailable: none.
