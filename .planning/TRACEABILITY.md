# Traceability

Statuses: **PROVEN** has concrete evidence, **ASSERTED** is claimed complete without located proof, **OPEN** is not complete, and **WAIVED** is an explicitly accepted gap.

Independent close-out sweep 2026-07-24. Structure lint exit 0. `npm run check` (typecheck + full tmux integration suite + `npm audit --omit=dev`) re-run fresh by the auditor: exit 0, all five suites passed, 0 vulnerabilities. Evidence below is located in the named suites and freshly reproduced by that run unless noted.

| Requirement | Description | Status | Evidence |
|---|---|---|---|
| REQ-001 | Commands keep a real PTY with durable complete output and exit state | **PROVEN** | `test-manager.mjs:89-112` starts a job asserting `[ -t 0 ] && [ -t 1 ] && [ -t 2 ]`, exit 23; verifies state `exited`, exitCode 23, and durable `output.log` containing ordered `req001-first` < `req001-last`, `req001-tty-ok`, and `finished ... exit=23`. Freshly reproduced 2026-07-24 |
| REQ-002 | Separate structured launcher supports Pi, Claude Code, and Hermes | **PROVEN** | `test-extension.mjs:63-64` asserts `tmux_agent` registered; `:135-237` drives all three backends × interactive/dispatch through fake executables, asserts backend/mode details, command construction, and owned-job discovery via `tmux_job list`; `buildAgentCommand` table checked `:53-55`. Freshly reproduced 2026-07-24 |
| REQ-003 | Prompt transport is private where supported and documents the Hermes argv exception | **PROVEN** | `test-extension.mjs:209-232` reads live `/proc/$$/cmdline` argv (sentinel absent for pi/claude, present for hermes), asserts `input.txt` equals prompt at mode 0600, and `command.sh`/`metadata.json`/pane metadata omit the sentinel; tool contract exception asserted `:70`, launch warning `:157-160`. `agent-adapters.ts:50` confirms Hermes `--oneshot "$(<...)"` argv exposure vs pi/claude stdin transport. Freshly reproduced 2026-07-24 |
| REQ-004 | Adapters preserve native permissions and surface Hermes one-shot auto-bypass | **PROVEN** | `test-extension.mjs:52-55` asserts no `--yolo`/`--dangerously-skip-permissions`/`--dangerously-bypass-approvals-and-sandbox` across every backend/mode command; Hermes launch warning `:157-160`; README exception `:72-73`. `test-hermes-config.mjs:36-39` re-asserts absence of isolation/bypass flags. Freshly reproduced 2026-07-24 |
| REQ-005 | Interactive agents remain observable and controllable through tmux | **PROVEN** | `test-extension.mjs:163-195` runs long-lived TTY fakes for pi/claude/hermes through bounded wait (timedOut true while running), send, tail (`fake-received=<input-…>`), interrupt, then completed wait asserting `fake-input=<tty>`, `fake-interrupted`, exit 130. Freshly reproduced 2026-07-24 |
| REQ-006 | Dispatch completion notifies the originating live session exactly once | **PROVEN** | `test-notifier.mjs` proves single delivery + duplicate suppression (waits==1), bounded content/logPath, backend/exit fields, `followUp`/`triggerTurn` options, idempotent shutdown abort, and post-shutdown watch rejection. `test-extension.mjs:153-155` confirms dispatch returns while `launching|running`; `:65-66` asserts exactly one `session_shutdown` cleanup handler; `:238` invokes reload cleanup. Located boundary: end-to-end delivery into a live Pi turn is exercised via the notifier unit (fake `notify`) plus wired `pi.sendMessage` in `index.ts:57`, not a live turn. Freshly reproduced 2026-07-24 |
| REQ-007 | Existing generic tmux job behavior and safeguards remain compatible | **PROVEN** | `test-manager.mjs` covers unowned-pane filtering (`:54-69`), duplicate-name rejection (`:71-74`), start/wait/capture/send, running-job close refusal (`:125`), interrupt, and forced close (`:134-145`); `test-extension.mjs:99-111` proves bounded output (`Output truncated`, <55KB). Freshly reproduced 2026-07-24 |
| REQ-008 | Fresh installs expose and document tool surfaces, model routing, and boundaries | **PROVEN** | `test-package.mjs` packs v1.1.0, installs offline into a clean root, asserts all runtime files/manifest, `pi.extensions`, `manifest.provides.tools == [tmux_job, tmux_agent]`, README coverage of backends, `litellm/deep`, config inheritance, Hermes exception, watcher lifecycle, and `## Non-goals`, and discovers both tools from the installed package. Freshly reproduced 2026-07-24 |
| REQ-009 | Pi validates and routes GPT and local LiteLLM model selections | **PROVEN** | `test-extension.mjs:114-133` rejects a missing model (`not present in the live registry`) and ambiguous `deep` (`Ambiguous Pi model deep`); valid `litellm/deep` yields argv `--model litellm/deep --thinking high` (`:192,198-201`). `model-registry.ts` requires exact `provider/model`. Live smoke freshly reproduced 2026-07-24: `pi --list-models` returns `litellm/deep`, `litellm/deep-think`, `litellm/fast`, and `openai-codex/gpt-5.6-sol`, all parseable and routed through Pi (no Codex CLI launch path exists in `agent-adapters.ts`) |
| REQ-010 | Hermes retains the operator's native configuration and customizations | **PROVEN** | `test-hermes-config.mjs` uses an isolated `HOME` and executable override; both interactive and dispatch commands read the fixture `.hermes/config.yaml` (`config=fixture_config: inherited`) while omitting `--safe-mode`/`--ignore-user-config`/`--ignore-rules`/`--yolo`/`--no-plugins`/`--disable-plugins`; args are `<>` interactive and `<--oneshot><fixture prompt>` dispatch. Freshly reproduced 2026-07-24 |

## Summary

- PROVEN: 10 — REQ-001 … REQ-010
- ASSERTED: 0
- OPEN: 0
- WAIVED: 0
- Freshly reproduced: full `npm run check` (typecheck, `test-manager`, `test-hermes-config`, `test-notifier`, `test-package`, `test-extension`), structure lint, and live `pi --list-models` parse — all 2026-07-24.
- Located only / not independently rerun: none beyond the noted REQ-006 live-turn delivery boundary.
- Unavailable: none.
