# Traceability

Statuses: **PROVEN** has concrete evidence, **ASSERTED** is claimed complete without located proof, **OPEN** is not complete, and **WAIVED** is an explicitly accepted gap.

Post-audit release-identity hygiene (executor, not a new independent verdict): `package.json`, `package-lock.json`, `extension-manifest.json`, and the packed-package version assertion now consistently report `1.5.0`; fresh `npm run check` exits 0 with all twelve suites and 0 vulnerabilities.

Independent **v1.5-parent-scoped-status close-out RE-AUDIT** 2026-07-25 (frontier auditor, Opus 4.8 `claude-opus-4-8[1m]` invoked via the project `close_out_auditor`; authored no v1.5 or prior slice). This is a second, independent close-out run after the executor repaired the sibling-hermetic real-TUI assertion that caused the historical FAIL. All requirement marks, checked boxes, and the pre-existing PROVEN labels below were treated as claims and re-verified against the working-tree diff vs the milestone start (`227b4cc`, v1.4.1 release), source, tests, the real Pi SDK internals, package/docs, and fresh gate/lint runs. **Verdict: PASS** — see `.planning/AUDIT.md` `## Audit: v1.5-parent-scoped-status (RE-AUDIT after repair)`. The historical FAIL and both its prior sections are retained verbatim in `.planning/AUDIT.md`.

Saga structure lint (`saga-lint .`) re-run fresh: exit 0, spine parses clean, no findings. Typecheck (`tsc --noEmit`) re-run fresh: exit 0.

Gate — the configured `npm run check` was re-run fresh **from the owned auditor pane, under the exact adverse condition that produced the historical FAIL**, and is now deterministically green:

- **In-session / owned-auditor-pane path (the actual close-out condition): PASS, deterministic 3/3.** This auditor runs inside owned pane `%1663` (`@pi_tmux_job_name=v15-closeout-reaudit`, `@pi_tmux_job_id=v15-closeout-reaudit-ms0pdhbo-ce0627`, `@pi_tmux_job_dir` set, `@pi_tmux_job_origin` **empty**, session `$0`, window `pi-jobs`), dispatched through the very extension under test. Because `TmuxJobManager.list()` enumerates every pane in the tmux session carrying `@pi_tmux_job_id`/`@pi_tmux_job_dir` (`job-manager.ts:326-361`) and the origin filter deliberately does not hide origin-less panes, the test's manager genuinely sees the auditor's own origin-less pane as a running sibling. `npm run check` from this pane exits **0** (initial run + 2 reruns): typecheck, all twelve suites including `real Pi InteractiveMode running, exited, closed, and stop status proof passed` and `stable bounded parent-scoped job-status projection tests passed`, and `npm audit --omit=dev` = `found 0 vulnerabilities`. This is the exact invocation path that deterministically exited 1 (4/4) before the repair.
- **Isolated fresh-session path (`env -u TMUX -u TMUX_PANE bash scripts/test-in-tmux.sh`): also PASS** (all twelve suites) — retained for completeness; the close-out verdict now rests on the owned-auditor-pane PASS above, not on the isolated path.

**The prior critical is fixed — verified, not inferred.** The historical FAIL was an unconditional `assert has(STATUS_UI_KEY) === false` / `has(STATUS_WIDGET_KEY) === false` after closing the test's own job, which any surviving owned sibling breaks. The repair (`test-real-tui-status.mjs`) threads `originPane: manager.originPane` + `successVisibilityMs: 3_000` into the monitor, waits for the test's own successful line to quietly expire, asserts the pane is still open (`(await manager.resolve(lifecycleName))?.paneId === waited.job.paneId` — a real REQ-026 non-destructive-expiry check), closes its job, then computes `remainingView = projectJobStatus(await manager.list(), undefined, { originPane, successVisibilityMs })` and asserts **either** cleared surfaces when `remainingView.statusText === undefined` **or** `statusText(mode) === remainingView.statusText` with `footerRendersPublishedStatus(mode) === true` when a survivor remains (`:172-182`). This is stronger than the v1.4.1 remedy because it is keyed on production's own `projectJobStatus` output rather than a hardcoded count.

**The sibling-tolerant branch is genuinely exercised (independently confirmed).** A read-only probe replicating the test's manager against the live session returned: `manager.originPane == "%1663"`; `manager.list()` = exactly the auditor's own pane `%1663` (`state=running`, `origin=undefined`, `acknowledged=false`); `projectJobStatus(jobs, undefined, { originPane: "%1663", successVisibilityMs: 3000 }).statusText == "tmux: 1 running"` — non-undefined, so the test takes the surviving-aggregate branch and asserts the real `FooterComponent.render()` contains that aggregate. The origin-less sibling the design intentionally does not filter is thus tolerated by matching the actual projection, not by assuming suppression.

**Production code is sound and additive.** The v1.5 production delta vs `227b4cc`: `job-manager.ts` tags panes with `@pi_tmux_job_origin`, projects `originPane`/`completedAt` (exit-code file mtime)/`acknowledged` (marker presence), and adds a running-refusing, non-destructive `acknowledge` (mode-0600 marker, pane untouched); `job-status.ts` origin-filters only the passive projection, retains untagged legacy panes, reclassifies nonzero exits as attention (sticky), and expires only successful exits after `SUCCESS_STATUS_VISIBILITY_MS = 30_000`; `job-status-ui.ts` threads `originPane`/`successVisibilityMs` into the projection; `index.ts` adds the `acknowledge` action. `list`/`resolve`/duplicate-name/close/cleanup remain session-wide. Every non-status core module (`job-status-monitor.ts`, `agent-adapters.ts`, `model-registry.ts`, `completion-notifier.ts`, `log-writer.mjs`, `workspace-manager.ts`) is **byte-identical to `227b4cc`** (verified `git diff --quiet`), so the REQ-001…REQ-024 posture carries forward and re-proves green in the same 12-suite gate. Two non-blocking pre-release warnings stand: the package version is still `1.4.1` (no bump for a milestone that adds a new tool action — no external collision since there are no tags and it is unpublished, but bump to `1.5.0` before release), and STATE.md still describes the pre-repair ASSERTED status and should be reconciled at the flip. `diag-tmp.mjs` remains an inert untracked diagnostic, left in place per this brief.

| Requirement | Description                                                                      | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------- | -------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ-001     | Commands keep a real PTY with durable complete output and exit state             | **PROVEN** | `test-manager.mjs` starts a job asserting `[ -t 0 ] && [ -t 1 ] && [ -t 2 ]`, exit 23; verifies `state=exited`, exitCode 23, durable `output.log` with ordered `req001-first` < `req001-last`, `req001-tty-ok`, and `[tmux-job] finished=… exit=23`. Drain protocol in `job-manager.ts` gates state flip on logger-ready and pipe-drained sentinels. Regression-green in the fresh 2026-07-25 gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| REQ-002     | Separate structured launcher supports Pi, Claude Code, and Hermes                | **PROVEN** | `test-extension.mjs` asserts `tmux_agent` registered without a `maxLogBytes` override; backend×mode command construction and owned-job discovery exercised through fake executables; `index.ts` registers a distinct `tmux_agent` tool that reuses the owned-pane lifecycle via `manager.start`. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| REQ-003     | Prompt transport is private where supported; documents the Hermes argv exception | **PROVEN** | `agent-adapters.ts` feeds pi/claude prompts over stdin (`input.txt`, mode 0600 in `job-manager.ts`) while Hermes `--oneshot "$(<…)"` exposes argv; `test-extension.mjs` inspects live `/proc/$$/cmdline` through the drained durable log (independent of terminal width) and persisted artifacts; README + `index.ts` surface the exception. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| REQ-004     | Adapters preserve native permissions and surface Hermes one-shot auto-bypass     | **PROVEN** | `test-extension.mjs` asserts no bypass flags across every backend/mode; `test-hermes-config.mjs` re-asserts absence of isolation/bypass flags; Hermes dispatch warning in `index.ts`. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| REQ-005     | Interactive agents remain observable and controllable through tmux               | **PROVEN** | `test-extension.mjs` runs long-lived TTY fakes through bounded wait (timedOut while running), send, tail, interrupt, then completed wait asserting exit 130; controls in `job-manager.ts`. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| REQ-006     | Dispatch completion notifies the originating live session exactly once           | **PROVEN** | `test-notifier.mjs` proves single delivery + duplicate suppression, bounded content/`logPath`, backend/exit fields, `deliverAs:"followUp"`/`triggerTurn`, idempotent shutdown abort (`completion-notifier.ts`); `test-extension.mjs` confirms dispatch returns while launching/running. The prior "no observed live turn" boundary is closed by REQ-014 (real AgentSession). Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| REQ-007     | Existing generic tmux job behavior and safeguards remain compatible              | **PROVEN** | `test-manager.mjs` covers unowned-pane filtering, duplicate-name rejection, start/wait/capture/send, running-job close refusal, interrupt→nonzero exit, forced close; `test-extension.mjs` proves bounded model-facing output (`Output truncated`, <55KB). The new additive `agent` metadata field does not appear on generic jobs (`test-manager.mjs` asserts `completed.agent === undefined` and durable `metadata.agent === undefined`). Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| REQ-008     | Fresh installs expose and document tool surfaces, model routing, and boundaries  | **PROVEN** | `test-package.mjs` packs v1.4.0, installs offline into a clean root, asserts runtime files, `manifest.provides.tools == [tmux_job, tmux_agent]`, `manifest.version == packageJson.version == "1.4.0"`, README coverage of backends, `litellm/deep`, `PI_TMUX_JOB_MAX_LOG_BYTES`, config inheritance, Hermes exception, `## Non-goals`, and discovers both tools. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| REQ-009     | Pi validates and routes GPT and local LiteLLM model selections                   | **PROVEN** | `test-extension.mjs` rejects missing/ambiguous models and routes valid `litellm/deep` through Pi; `model-registry.ts` enforces exact `provider/model`; `agent-adapters.ts` has no Codex CLI launch path. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| REQ-010     | Hermes retains the operator's native configuration and customizations            | **PROVEN** | `test-hermes-config.mjs` uses isolated `HOME` + executable override; both commands read the fixture `.hermes/config.yaml` while omitting `--safe-mode`/`--ignore-user-config`/`--ignore-rules`/`--yolo`/`--no-plugins`/`--disable-plugins`. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| REQ-011     | Operator policy bounds retained per-job logs during execution                    | **PROVEN** | `log-writer.mjs` `writeBounded` holds `output.log` at ≤`maxBytes` after every chunk; `test-log-writer.mjs` byte-exactly verifies single-write and chunked tails; `test-manager.mjs` samples on-disk `output.log*` 30× under a 4096 cap during real-tmux output and proves unlimited growth + invalid-value rejection. Policy is env/constructor-only; neither tool exposes `maxLogBytes`. Source unchanged since v1.2. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| REQ-012     | Capped logging preserves PTY/completion behavior and reports truncation          | **PROVEN** | Under a 4096 cap `test-manager.mjs` gates on stdin/stdout/stderr all being TTYs, emits `REQ012_TTY_OK` in the retained tail after sustained output, asserts that marker in both the drained durable log and pane capture, and proves finished marker, `maxLogBytes=4096`, `logTruncated=true`, `log-truncated` marker, and `metadata.json.maxLogBytes=4096`; drain-before-exit gate unchanged. Reporting surfaced through `describeJob`, completion notice, tool output, and README. Source unchanged since v1.2. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| REQ-013     | Repeated small over-budget writes retain a byte-exact chronological suffix       | **PROVEN** | `test-log-writer.mjs` streams ten distinct 71-byte chunks under a 128-byte cap, byte-checking the newest-128 suffix after every chunk (nine genuine case-3 over-budget rewrites), then asserts final file == newest 128 B of the 710-B stream, `truncated`/`drained` markers. `log-writer.mjs` unchanged since v1.2. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| REQ-014     | Live Pi completion delivery, follow-up turn, and shutdown suppression            | **PROVEN** | `test-live-notification.mjs` loads the packaged extension into a real in-memory Pi `AgentSession`, dispatches `tmux_agent` against a real tmux child, and asserts exactly one `tmux-agent-completion` custom message ingested into the follow-up model context with exactly one additional agent run/model call, plus a genuine post-`reload()`/`session_shutdown` suppression proof. No production source changed vs HEAD. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| REQ-015     | Dispatch records and reports Git/worktree identity and intent                    | **PROVEN** | `workspace-manager.ts` resolves non-Git, main/linked-worktree, branch/detached, and dirty state; `job-manager.ts` additively persists/validates workspace metadata while accepting historical records; `index.ts` exposes read/write intent and reports workspace context. `test-workspace.mjs` + `test-extension.mjs` cover all cases; packed-package test includes the module. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| REQ-016     | Auto policy safely arbitrates read/write workspace sharing and isolation         | **PROVEN** | `decideWorkspace` covers read sharing, sole writers, dirty refusal, explicit-current override, isolation, same-tree conflicts, non-Git refusal, and separate-worktree independence; `WorkspaceAllocator` serializes decisions. `test-workspace.mjs` proves the policy matrix and mutex; `test-extension.mjs` proves live clean reuse, dirty refusal, and concurrent-writer isolation. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| REQ-017     | Managed worktrees have safe creation, ownership, and rollback                    | **PROVEN** | `ManagedWorkspaceManager` creates collision-safe `pi-tmux/*` branches at inspected HEAD under an operator-only root, writes mode-0600 ownership records under mode-0700 dirs, records managed identity, rejects dirty/non-Git inputs, and rolls back ownership-verified on launch failure. Temporary-repository + real-extension tests prove identity, permissions, records, and rollback. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| REQ-018     | Explicit cleanup removes only verified safe owned worktrees                      | **PROVEN** | `tmux_job cleanup-workspace` requires a stopped managed job, refuses running/current/outside-root/missing/mismatched ownership, preserves dirty trees, removes clean owned worktrees, atomically deletes only unchanged owned-base branches, retains committed branches, writes tombstones, and is idempotent. `test-workspace.mjs` + `test-extension.mjs` prove all refusal/preservation/clean-delete/repeat paths. Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| REQ-019     | Workspace safety is documented, packaged, and regression-proven                  | **PROVEN** | README documents intent/mode policy, dirty/current boundaries, isolation, operator-only `PI_TMUX_WORKTREE_ROOT`, ownership-safe cleanup, committed-branch retention, tombstones, and no destructive override; fresh-package tests assert `workspace-manager.ts`, schemas, and reader contract; the full gate preserves all prior regressions with 0 vulnerabilities. Independent v1.3 audit returned PASS (`.planning/AUDIT.md`). Regression-green 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| REQ-020     | Agent metadata identifies backend and launch mode compatibly                     | **PROVEN** | `job-manager.ts` `AgentJobMetadata` additively stores strict Pi/Claude/Hermes backend and interactive/dispatch mode in durable `metadata.json` and `TmuxPaneJob`; `parseAgentMetadata` returns `undefined` for absent metadata (historical compat) and throws only on malformed present values. `index.ts` supplies `agent: { backend, mode }` for `tmux_agent`; generic `tmux_job` omits it. `test-manager.mjs` proves generic omission (`completed.agent === undefined`, durable `metadata.agent === undefined`), malformed refusal (`await assert.rejects(manager.list(), /Invalid agent in job metadata/)`), and missing-field round-trip (`resolve(...).agent === undefined`). `test-extension.mjs` asserts `started.details.job.agent`, durable `metadata.json.agent`, and listed `jobs.find(...).agent` all `deepEqual { backend, mode }` across every backend×mode through real tmux dispatch. Freshly reproduced 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| REQ-021     | Status projection is stable, bounded, and operationally complete                 | **PROVEN** | `job-status.ts` is a pure projection (`[...jobs].sort`, no input mutation): classifies launching/running vs exited vs attention; emits `tmux: N running · N exited · N attention` aggregate; sorts running(0)→attention(1)→exited(2) with name then id ties; reports marker + name, `backend/mode` or `cmd` identity, `state`/`exit=<code>`, `workspace=<kind>`, and `log=truncated`; caps at 4 by default with explicit `… +N more`; derives a stable `key`. `test-status.mjs` proves empty, active/exited success/failure, attention (`log-failed:exit=2`), generic/agent identity, current/managed workspace, truncation, overflow at limits 2 and 0, input-immutability (`originalOrder` unchanged), reorder-invariance, and key stability. Module packaged in `package.json` `files`. Freshly reproduced 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| REQ-022     | Session monitor is bounded, deduplicated, and leak-free                          | **PROVEN** | `job-status-monitor.ts` `JobStatusMonitor` starts idempotently (`if (this.active) return`) with an immediate refresh plus injected scheduler, enforces `Math.max(100, …)` minimum cadence, permits one in-flight poll (`inFlight` guard), publishes only changed `view:`/`error:` keys, normalizes+bounds error text to 240 chars, republishes recovery after an error, cancels its injected timer and emits one `clear` on stop, and suppresses late results by `generation`. It starts no timer at module load — only in `start()`. `test-status-monitor.mjs` uses a fake scheduler + deferred promises to prove min-interval (100), immediate+idempotent start, overlap suppression, view dedup, identical-error dedup, recovery republish, cancel/clear, and post-stop late-result + dead-callback suppression. Module packaged. Freshly reproduced 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| REQ-023     | Footer aggregate and detail widget integrate without replacing core UI           | **PROVEN** | `job-status-ui.ts` starts a monitor only for `ctx.mode === "tui"` (returns `undefined` otherwise), renders the aggregate via keyed themed `setStatus(STATUS_UI_KEY, theme.fg("accent", …))`, renders bounded details via keyed `setWidget(STATUS_WIDGET_KEY, lines, { placement: "belowEditor" })`, clears both on empty/`clear`, and on polling error shows `theme.fg("warning","tmux: unavailable")` while clearing the widget; it never calls `setFooter` or replaces the editor (grep-confirmed absent). `index.ts` creates the monitor in `session_start`, replaces any prior monitor, and stops/clears it alongside notifier cleanup in a single shared `session_shutdown` handler. Real Pi SDK 0.81.1 confirmed to provide the `setStatus`/`setWidget(belowEditor)`/`theme.fg` API and `session_start (_event, ctx)` signature. `test-status-ui.mjs` proves non-TUI inertness (no schedule, no list, no UI calls), exact themed status/widget calls with `belowEditor` placement, active-first detail content, empty-state and error clearing, and timer cancellation on stop; `test-extension.mjs` asserts exactly one `session_start` and one shared `session_shutdown` handler. Successful exits remain passive-visible for 30 seconds while failed exits stay attention-visible; explicit acknowledgment hides stopped-job status without closing the retained shell pane. Freshly reproduced 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| REQ-024     | Status feature is documented, packaged, and regression-proven                    | **PROVEN** | README `### Live job status` documents the TUI-only footer/widget, four-line+overflow bound, immediate-then-1s non-overlapping cadence, `tmux: unavailable` failure state, non-TUI no-timer behavior, and shutdown clearing; `package.json`/`extension-manifest.json`/`package-lock.json` are consistent at 1.4.0; `test-package.mjs` asserts all three status modules in the packed install, the README contract, `packageJson.version == manifest.version == "1.4.0"`, one `session_start` + one `session_shutdown` handler, and — the resource-leak check — zero `setInterval` calls during factory `discoverAndLoadExtensions` (`factoryIntervalCalls === 0`). The full eleven-suite gate preserves generic jobs, all agent backends, workspace safety, genuine PTYs, completion delivery, and bounded logs with 0 vulnerabilities. Independent frontier close-out audit reproduced the gate + structure lint, deeply traced REQ-020…REQ-024, swept REQ-001…REQ-019 for regressions, and returned PASS (`.planning/AUDIT.md`). Freshly reproduced 2026-07-25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| REQ-025     | Real Pi InteractiveMode renders and clears live tmux job status                  | **PROVEN** | `test-real-tui-status.mjs` builds a real `AgentSession` + Pi `InteractiveMode` without `init()`/`run()` (no raw terminal input) and reads the real `footerDataProvider`/`extensionWidgetsBelow` stores plus real `FooterComponent`/Container/Text `.render()`; `mode.ui.requestRender` is no-op'd to suppress only the out-of-scope terminal byte-paint. Running/exited state is asserted on the test's own rendered widget line (`/cmd · running · workspace=-/`, `cmd · exit=0`); after quiet successful expiry the pane is proven still open (`resolve().paneId` unchanged); the post-close surfaces are guarded with the production `projectJobStatus(await manager.list(), undefined, { originPane, successVisibilityMs })` — cleared only when no visible survivor remains, else the real footer must render the surviving aggregate (`statusText(mode) === remainingView.statusText`); monitor-stop clears both surfaces via the `JobStatusMonitor.stop()` invariant. Re-audited fresh from the owned, origin-less auditor pane `%1663` with the sibling genuinely visible to `list()`: the configured `npm run check` exits 0 deterministically (3/3), the proof passes, 0 vulnerabilities. A read-only probe confirmed the sibling-tolerant branch (`statusText == "tmux: 1 running"`) is the one exercised — the exact adverse condition that produced the historical FAIL, now handled. Freshly reproduced 2026-07-25.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| REQ-026     | Passive status is parent-scoped, quiet on success, and non-destructive           | **PROVEN** | Production behavior is additive and non-destructive, and the requirement's cited real-TUI + full-gate evidence now reproduces green under the independent close-out condition. `job-manager.ts` tags each new pane with `@pi_tmux_job_origin`, projects `originPane`/`completedAt` (exit-code file mtime)/`acknowledged` (marker presence) while keeping `list`/`resolve`/duplicate-name/close/cleanup session-wide; the running-refusing, non-destructive `acknowledge` writes a mode-0600 marker and keeps the pane open; `job-status.ts` origin-filters only the passive projection, retains untagged legacy panes, reclassifies nonzero exits as sticky attention, and expires only successful exits after `SUCCESS_STATUS_VISIBILITY_MS=30_000`. `test-status.mjs` proves origin isolation (foreign non-empty origin filtered, origin-less legacy retained), the 29 999/30 001 ms boundary, sticky `exit=4`→attention, acknowledgment filtering, and stable running→attention→exited ordering (`tmux: 2 running · 1 exited · 1 attention`); `test-manager.mjs` proves origin tagging, finite completion timestamp, running-ack refusal, durable acknowledgment, and `resolve().paneId` unchanged after ack (pane retained); `test-extension.mjs` proves the `acknowledge` action returns "passive status is hidden"/"remains open" with `details.job.acknowledged == true` and that `list` still returns the acknowledged job (session-wide management intact); `test-package.mjs` proves `acknowledge` in the packed action enum; the README documents origin-scoping, 30 s success expiry, non-destructive acknowledge, and legacy-pane compat. The real-InteractiveMode proof and the full `npm run check` now exit 0 deterministically (3/3) from the owned, origin-less auditor pane `%1663` with a sibling visible to `list()`; 0 vulnerabilities. See `.planning/AUDIT.md` `## Audit: v1.5-parent-scoped-status (RE-AUDIT after repair)` (PASS). Freshly reproduced 2026-07-25. |

## Summary

- PROVEN: 26 — REQ-001 … REQ-026
- ASSERTED: 0
- OPEN: 0
- WAIVED: 0
- Freshly reproduced (2026-07-25, v1.5 close-out RE-AUDIT, from owned auditor pane `%1663`): Saga structure lint (`saga-lint .`) exit 0, spine clean; `tsc --noEmit` exit 0.
- Gate PASSES under the actual close-out condition: `npm run check` from the owned, origin-less auditor pane `%1663` (`v15-closeout-reaudit`, session `$0`) exits 0 deterministically (3/3 = initial + 2 reruns) — all twelve suites including `real Pi InteractiveMode running, exited, closed, and stop status proof passed` and `stable bounded parent-scoped job-status projection tests passed`, and `npm audit --omit=dev` = 0 vulnerabilities. This is the exact invocation path that deterministically exited 1 (4/4) before the executor's repair.
- Prior FAIL resolved: the executor restored a sibling-tolerant guard (stronger than v1.4.1's — keyed on production's own `projectJobStatus` origin-scoped output rather than a hardcoded count). A read-only probe confirmed the auditor's origin-less pane `%1663` is genuinely visible to the test's `manager.list()` and drives the surviving-aggregate (`else`) branch — so the fix is verified under the live adverse condition, not merely inferred. Production projection code (`job-status.ts`, `job-manager.ts`) is additive; every non-status core module is byte-identical to `227b4cc`.
- Located only / not independently rerun: none.
- Non-blocking pre-release follow-ups (not audit blockers): bump the package version from `1.4.1` to `1.5.0` (and the `test-package.mjs` pin) for the milestone's new `acknowledge` tool action — no external collision (no git tags, unpublished on npm); reconcile STATE.md, which still describes the pre-repair ASSERTED status; remove the inert untracked `diag-tmp.mjs` before committing (left in place per this brief).
- Unavailable: none.
